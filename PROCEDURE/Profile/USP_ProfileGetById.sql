USE [Donne]
GO

SET ANSI_NULLS ON
GO

SET QUOTED_IDENTIFIER ON
GO

CREATE PROCEDURE [dbo].[USP_ProfileGetById] (@ProfileId INT)
AS
BEGIN
	SELECT TOP 1 * FROM [dbo].[Profile]
		WHERE ProfileId = @ProfileId;
	SET NOCOUNT ON;
END
GO


