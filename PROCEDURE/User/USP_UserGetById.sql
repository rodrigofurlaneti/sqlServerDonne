USE [Donne]
GO

SET ANSI_NULLS ON
GO

SET QUOTED_IDENTIFIER ON
GO

CREATE PROCEDURE [dbo].[USP_UserGetById] (@UserId INT)
AS
BEGIN
	SELECT TOP 1 * FROM [dbo].[User]
		WHERE UserId = @UserId;
	SET NOCOUNT ON;
END
GO


