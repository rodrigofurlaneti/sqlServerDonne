USE [Donne]
GO

SET ANSI_NULLS ON
GO

SET QUOTED_IDENTIFIER ON
GO

CREATE PROCEDURE [dbo].[USP_Donne_Command_GetAll] 
AS
BEGIN
	SET NOCOUNT ON;
	SELECT * 
		FROM Donne_Command
			WHERE Status = 1
			ORDER BY CommandId ASC;
END
GO